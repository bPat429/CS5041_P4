import { StyleSheet } from "react-native";

// Styles repurposed from 
const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        height: '100%',
        overflow: 'hidden'
    },
    background_container: {
        backgroundImage: 'linear-gradient(to left bottom, white, #ffa6bb, #ffaca7, #ffb795, white, #efd28d, #dfdd99, white, #c7eec6, #cbf2de, #daf3ee, white)', 
        flex: 1, 
        height: "100%",
        overflow: "hidden"
    }
});

export default styles;